<%# Hyper-basic profile page, just for debugging atm %>
<%= @user.name %>
<%= @user.email %>
<%# Debug information about the page can be displayed using the following: %>
    <p><br><strong>Debug Information:</strong></p>
    <%= debug(params) if Rails.env.development? %>